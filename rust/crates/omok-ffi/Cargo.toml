[package]
name = "omok-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "C ABI for the Omok AI core, for the LuaJIT bindings."

[lib]
name = "omok_ai"
crate-type = ["cdylib", "rlib"]

[dependencies]
omok-core = { workspace = true }
omok-mlx = { workspace = true }
