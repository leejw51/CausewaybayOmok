__pycache__/
*.pyc
.pytest_cache/
.venv/
runs/*
# ...except the shipped model (make game / make play load it)
!runs/base/
runs/base/*
!runs/base/checkpoints/
runs/base/checkpoints/*
!runs/base/checkpoints/best.npz
!runs/base/checkpoints/best.json
assets_raw/
*.mlpackage/
*.onnx
.DS_Store
runs.tar
# The macOS .app, its zip, and the LÖVE download `make app` stages.
love2d/build/
# `make package` output.
dist/
